import "../index.css";
import SocialLinks from "./SocialLinks";

const Header = () => {
  return (
    <footer className="flex justify-center mt-auto h-8 lg:justify-end">
      <SocialLinks />
    </footer>
  );
};

export default Header;
